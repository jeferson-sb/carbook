{
  "type": "postgres",
  "port": 5432,
  "host": "localhost",
  "username": "docker",
  "password": "postgres",
  "database": "carbook",
  "entities": ["./src/modules/**/typeorm/*.ts"],
  "migrations": ["./src/infrastructure/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/infrastructure/database/migrations"
  }
}
